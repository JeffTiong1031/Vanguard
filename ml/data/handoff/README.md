# Handoff — Task 14 audit pack

| File | Role |
|---|---|
| `draft_for_audit.jsonl` | 540 LLM-synthetic train candidates (`draft-001` … `draft-540`), ID-fixed and schema-validated. |

**Do not treat this as the final train set.** Humans must audit labels, then merge (see `docs/team/task-14-audit-handoff.md`).

`data/llm_draft/` remains gitignored for local scratch; this folder is the shareable snapshot for the team.
