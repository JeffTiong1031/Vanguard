[project]
name = "vanguard-classifier"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "scikit-learn>=1.5",
  "numpy>=1.26",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
